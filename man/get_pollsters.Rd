% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pollsters.R
\name{get_pollsters}
\alias{get_pollsters}
\title{Get pollsters}
\usage{
get_pollsters(
  election_key = NULL,
  territory_code = NULL,
  active_only = TRUE,
  limit = 100,
  offset = 0,
  collect_all = FALSE
)
}
\arguments{
\item{election_key}{Optional stable election key.}

\item{territory_code}{Optional territory code filter.}

\item{active_only}{If \code{TRUE}, count only active, non-manual-review polls.}

\item{limit}{Number of rows to request. Must be between 1 and 1000.}

\item{offset}{Zero-based row offset.}

\item{collect_all}{If \code{TRUE}, collect all pages starting at offset 0.}
}
\value{
A tibble with one row per pollster, ordered by descending
\code{poll_count}, then \code{pollster_name} and \code{pollster_key}.
}
\description{
Lists pollsters and matching poll counts.
}
\examples{
if (interactive()) {
  get_pollsters(election_key = "congreso_espana_2027")
}
}
